-- CREATE TABLE containers
-- (
--     id          serial primary key,
--     room_name   varchar(32) not null references rooms (name),
--     type        VARCHAR(32) NOT NULL,
--     description varchar(255),
--     height      integer,
--     width       integer,
--     length      integer
-- );
--
-- INSERT INTO containers(room_name, type, description)
-- VALUES ('closet', 'dresser', 'the things go here')